"""
Tryloop — Database seed script.
Populates the database with sample devices, users, and trials for development.
Will be implemented after the database models are built (step 2).
"""

# This script imports from the backend package, so run it from the project root:
#   python -m scripts.seed
# or inside Docker:
#   docker compose exec backend python /app/../scripts/seed.py
